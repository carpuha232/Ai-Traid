# 🤖 Версии торгового бота

Этот репозиторий содержит разные версии торгового бота для совместной работы.

## 📁 Структура

```
versions/
├── russian/      # Версия Руслана
└── colleague/    # Версия коллеги
```

## 🚀 Инструкции для коллеги

### Как загрузить свою версию:

1. **Склонируйте репозиторий:**
   ```bash
   git clone https://github.com/carpuha232/Ai-Traid.git
   cd Ai-Traid
   ```

2. **Скопируйте свою версию бота в папку `versions/colleague/`:**
   ```bash
   # Скопируйте все файлы вашего бота в versions/colleague/
   cp -r /путь/к/вашему/боту/* versions/colleague/
   ```

3. **Добавьте файлы в git:**
   ```bash
   git add versions/colleague/
   git commit -m "Добавлена версия коллеги"
   git push origin main
   ```

### Как скачать версию Руслана:

```bash
git pull origin main
# Версия Руслана будет в папке versions/russian/
```

## 📝 Правила работы

- **Не изменяйте** файлы в папке `versions/russian/` без согласования
- **Не изменяйте** файлы в папке `versions/colleague/` без согласования
- Каждый работает в своей папке
- Для обсуждения изменений используйте Issues или Pull Requests

## 🔧 Запуск версии

### Версия Руслана:
```bash
cd versions/russian
python main.py
```

### Версия коллеги:
```bash
cd versions/colleague
python main.py
```

## 📞 Контакты

- GitHub Issues: https://github.com/carpuha232/Ai-Traid/issues

