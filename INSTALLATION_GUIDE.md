# 🚀 УСТАНОВКА НА ДРУГОМ ПК

**Версия:** Snapshot 2025-11-12 12:52  
**Коммит:** `2990a12`

---

## 📋 **Требования к системе:**

### **Операционная система:**
- ✅ Windows 10/11
- ✅ Linux (Ubuntu 20.04+)
- ✅ macOS (10.15+)

### **Программное обеспечение:**
- Python 3.9+ (рекомендую 3.10 или 3.11)
- Git
- Интернет-соединение

---

## 📥 **Шаг 1: Установка Python**

### **Windows:**
1. Скачать: https://www.python.org/downloads/
2. При установке ✅ **отметить "Add Python to PATH"**
3. Проверить: открыть PowerShell и ввести:
   ```powershell
   python --version
   ```
   Должно показать: `Python 3.10.x` или выше

### **Linux:**
```bash
sudo apt update
sudo apt install python3.10 python3-pip git
python3 --version
```

### **macOS:**
```bash
brew install python@3.10 git
python3 --version
```

---

## 📥 **Шаг 2: Установка Git**

### **Windows:**
1. Скачать: https://git-scm.com/download/win
2. Установить с настройками по умолчанию
3. Проверить:
   ```powershell
   git --version
   ```

### **Linux:**
```bash
sudo apt install git
```

### **macOS:**
```bash
brew install git
```

---

## 📦 **Шаг 3: Клонирование проекта**

### **Вариант А: Через командную строку**

1. Открыть терминал (PowerShell/Terminal)
2. Перейти в папку где хотите разместить проект:
   ```bash
   cd C:\Users\YOUR_NAME\Projects    # Windows
   cd ~/Projects                      # Linux/Mac
   ```

3. Клонировать репозиторий:
   ```bash
   git clone https://github.com/carpuha232/Ai-Traid.git
   cd Ai-Traid
   ```

4. **ВАЖНО!** Переключиться на нужную версию:
   ```bash
   git checkout 2990a12
   ```
   Или остаться на последней версии (main):
   ```bash
   git checkout main
   ```

### **Вариант Б: Через GitHub Desktop**

1. Скачать GitHub Desktop: https://desktop.github.com/
2. Установить и войти в аккаунт
3. File → Clone Repository
4. Вставить: `https://github.com/carpuha232/Ai-Traid`
5. Выбрать папку и нажать "Clone"

### **Вариант В: Скачать ZIP архив**

1. Открыть: https://github.com/carpuha232/Ai-Traid
2. Code → Download ZIP
3. Распаковать в нужную папку

⚠️ **Минус:** нельзя легко обновляться через git pull

---

## 🔧 **Шаг 4: Установка зависимостей**

### **Windows (PowerShell):**
```powershell
# Перейти в папку проекта
cd C:\Users\YOUR_NAME\Projects\Ai-Traid

# Создать виртуальное окружение (рекомендуется)
python -m venv venv
.\venv\Scripts\Activate.ps1

# Если ошибка ExecutionPolicy:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Установить зависимости
pip install -r requirements.txt
```

### **Linux/macOS:**
```bash
# Перейти в папку проекта
cd ~/Projects/Ai-Traid

# Создать виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### **Что установится:**
- PySide6 (GUI)
- websockets (Binance WebSocket)
- python-binance (Binance API)
- aiohttp (async HTTP)
- numpy (математика)
- и другие...

**Время установки:** 2-5 минут

---

## ⚙️ **Шаг 5: Настройка конфигурации**

### **5.1. API ключи Binance (опционально для demo)**

Для **demo/paper trading** (без реальных денег) - **ключи НЕ нужны!**

Для **live trading** (реальные деньги):

1. Открыть `config.json`
2. Заменить:
   ```json
   "api": {
     "key": "ВАШ_BINANCE_API_KEY",
     "secret": "ВАШ_BINANCE_API_SECRET"
   }
   ```

3. Как получить ключи:
   - Зайти на Binance.com
   - Profile → API Management
   - Create API
   - ✅ Enable Futures Trading
   - ❌ Enable Withdrawals (ОТКЛЮЧИТЬ!)
   - Скопировать Key и Secret

### **5.2. Режим торговли**

В `config.json`:
```json
"mode": "paper_trading"  // Демо-режим (безопасно!)
// "mode": "live"        // Реальные деньги (осторожно!)
```

### **5.3. Стартовый баланс (demo)**

```json
"account": {
  "starting_balance": 1000  // $1000 в демо-режиме
}
```

---

## 🚀 **Шаг 6: Запуск бота**

### **Windows:**
```powershell
# В папке проекта с активированным venv:
python main.py
```

### **Linux/macOS:**
```bash
python3 main.py
```

### **Что должно произойти:**

1. Откроется GUI окно "Scalping Bot"
2. В консоли:
   ```
   ✅ All components initialised
   🔌 Connecting to Binance...
   🚀 Started 60 WebSocket streams for 20 pairs
   ✅ Connected to Binance WebSocket
   🚀 Bot is running and monitoring the market...
   ```

3. Через 10-30 секунд начнут появляться сигналы в "SIGNAL SCANNER"

---

## ✅ **Проверка что всё работает:**

### **1. Проверка подключения:**
- Статус внизу: "Connected" (зелёный)
- Кнопка "Подключить" зелёная

### **2. Проверка сигналов:**
- В "SIGNAL SCANNER" появляются записи:
  ```
  18:52:30 ✅ BTCUSDT: LONG сигнал 67.5%
           • Prob UP: 67.5% | Prob DOWN: 32.5%
           • Bull: 5 | Bear: 2
  ```

### **3. Проверка торговли:**
- Включить "Авто ВКЛ" (кнопка станет зелёной)
- Через 30-60 секунд в "OPEN POSITIONS" появятся позиции

### **4. Проверка баланса:**
- Верхняя панель: Баланс: $1,000
- PNL обновляется в реальном времени

---

## 🛠️ **Частые проблемы и решения:**

### **Проблема 1: `ModuleNotFoundError: No module named 'PySide6'`**

**Решение:**
```bash
pip install PySide6
```
Или переустановить всё:
```bash
pip install -r requirements.txt --force-reinstall
```

---

### **Проблема 2: Окно не открывается (Windows)**

**Решение:**
```powershell
# Проверить что Python видит PySide6:
python -c "import PySide6; print('OK')"

# Если ошибка - переустановить:
pip uninstall PySide6
pip install PySide6==6.7.0
```

---

### **Проблема 3: `ConnectionRefusedError` или нет сигналов**

**Причины:**
- Нет интернета
- Binance заблокирован в вашей стране

**Решение:**
- Проверить интернет
- Использовать VPN (Binance может быть недоступен в некоторых странах)

---

### **Проблема 4: `PermissionError` при запуске (Windows)**

**Решение:**
```powershell
# Разрешить выполнение скриптов:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

### **Проблема 5: Высокая нагрузка на CPU/память**

**Причины:**
- 20 пар × 3 WebSocket = 60 соединений
- GUI обновляется каждую секунду

**Решение:**
В `config.json` уменьшить количество пар:
```json
"pairs": [
  "BTCUSDT", "ETHUSDT", "BNBUSDT", "SOLUSDT", "XRPUSDT"
  // Оставить только 5-10 пар
]
```

---

## 📱 **Обновление до новой версии**

### **Если установлено через Git:**

```bash
# Сохранить изменения (если были):
git add -A
git commit -m "My changes"

# Получить обновления:
git pull origin main

# Обновить зависимости (если изменились):
pip install -r requirements.txt --upgrade
```

### **Если скачан ZIP:**
- Скачать новый ZIP
- Сохранить свой `config.json`
- Заменить все файлы кроме `config.json`

---

## 🔄 **Восстановление конкретной версии**

### **Вернуться к снапшоту 2025-11-12:**

```bash
git fetch origin
git checkout 2990a12
```

### **Вернуться к последней версии:**

```bash
git checkout main
git pull
```

---

## 📞 **Поддержка**

**Если что-то не работает:**

1. Проверить логи в папке `logs/`
2. Посмотреть вывод в консоли
3. Открыть Issue на GitHub:
   https://github.com/carpuha232/Ai-Traid/issues

**При создании Issue указать:**
- Операционную систему
- Версию Python (`python --version`)
- Текст ошибки из консоли/логов
- Что делали перед ошибкой

---

## ⚡ **Быстрый старт (TL;DR):**

```bash
# 1. Клонировать
git clone https://github.com/carpuha232/Ai-Traid.git
cd Ai-Traid

# 2. Установить зависимости
python -m venv venv
source venv/bin/activate  # Linux/Mac
.\venv\Scripts\Activate.ps1  # Windows
pip install -r requirements.txt

# 3. Запустить
python main.py
```

**Готово! Бот запущен!** 🚀

---

## 📝 **Checklist перед запуском:**

- [ ] Python 3.9+ установлен
- [ ] Git установлен
- [ ] Проект клонирован
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] `config.json` настроен (для demo можно не менять)
- [ ] Интернет подключён
- [ ] Запущен `python main.py`
- [ ] GUI открылось
- [ ] Статус "Connected"
- [ ] Сигналы появляются в логе

**Если всё ✅ - можно торговать!**

